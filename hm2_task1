#include <iostream>
using namespace std;
int odd, obr, start;
int perevert(int start) {
  while (start > 0) {
    odd = start % 10;
    start = start / 10;
    obr = obr*10 + odd;
    }
  return obr;  
}
int main() {
   cout << "Введите число" << endl;
   cin >> start;
   cout << "Перевернутое число: " << perevert(start) << endl;
}
